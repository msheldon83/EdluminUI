query GetOrganizationRelationships($orgId: ID!) {
  organizationRelationship {
    all(orgId: $orgId) {
      id
      orgId
      relatesToOrganizationId
      relationshipType
      relatesToOrganization {
        id
        name
      }
    }
  }
}
