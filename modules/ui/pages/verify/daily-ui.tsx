import * as React from "react";
import { AssignmentRow } from "./types";
import { ProgressBar } from "./components/progess-bar";

type Props = {
  assignments: AssignmentRow[];
};

export const VerifyDailyUI: React.FC<Props> = ({ assignments }) => {
  return <></>;
};
