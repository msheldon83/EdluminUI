query GetAbsenceReasonCategory($absenceReasonCategoryId: ID!) {
  orgRef_AbsenceReasonCategory {
    byId(id: $absenceReasonCategoryId) {
      id
      externalId
      rowVersion
      name
      description
      validUntil
      absenceReasonTrackingTypeId
      allowNegativeBalance
      expired
    }
  }
}
