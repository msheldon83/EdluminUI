query GetAllPermissionSetsWithinOrg($orgId: ID, $roles: [OrgUserRole]) {
  permissionSet {
    all(orgId: $orgId, roles: $roles) {
      id
      name
      rowVersion
      externalId
      description
      orgUserRole
      permissionSet
    }
  }
}
