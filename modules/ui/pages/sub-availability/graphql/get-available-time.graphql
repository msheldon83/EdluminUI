query GetMyAvailableTime {
  userAccess {
    me {
      user {
        id
        rowVersion
        availableTime {
          id
          availabilityType
          daysOfWeek
          availableTime
        }
      }
    }
  }
}
