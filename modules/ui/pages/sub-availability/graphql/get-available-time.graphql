query GetMyAvailableTime {
  userAccess {
    me {
      id
      user {
        id
        rowVersion
        availableTime {
          id
          availabilityType
          daysOfWeek
          availableTime
        }
      }
    }
  }
}
