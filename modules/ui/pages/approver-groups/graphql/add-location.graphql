mutation AddApproverGroupForLocation(
  $approverGroup: ApproverGroupCreateInput!
) {
  approverGroup {
    createApproverGroupForLocation(approverGroup: $approverGroup) {
      id
      name
    }
  }
}
