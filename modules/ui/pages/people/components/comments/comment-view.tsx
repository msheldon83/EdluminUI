import * as React from "react";

type Props = {
  // commenter: string;
  // commentDate: Date;
  // district: string;
  // payload: string;
};

export const CommentView: React.FC<Props> = props => {
  return <></>;
};
