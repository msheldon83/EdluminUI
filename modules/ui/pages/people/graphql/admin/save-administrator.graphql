mutation SaveAdmin($administrator: AdministratorInput!) {
  orgUser {
    saveAdministrator(administrator: $administrator) {
      id
      externalId
      userId
      loginEmail
      administrator {
        ...AdminDetails
      }
    }
  }
}
