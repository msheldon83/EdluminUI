query GetSubstituteById($id: ID!) {
  orgUser {
    byId(id: $id) {
      id
      userId
      loginEmail
      rowVersion
      relatedOrganizations {
        id
        name
      }
      substitute {
        ...SubstituteDetails
      }
    }
  }
}
