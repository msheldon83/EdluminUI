query GetSubstituteById($id: ID!) {
  orgUser {
    byId(id: $id) {
      id
      externalId
      userId
      loginEmail
      relatedOrganizations {
        id
        name
      }
      substitute {
        ...SubstituteDetails
      }
    }
  }
}
