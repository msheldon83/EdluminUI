query GetAllPeopleForOrg(
  $orgId: ID!
  $limit: Int
  $offset: Int
  $sortBy: [SortDetailsInput]
  $role: [OrgUserRole]
  $active: Boolean
  $name: String
) {
  orgUser {
    paged(
      orgId: $orgId
      limit: $limit
      offset: $offset
      sortBy: $sortBy
      role: $role
      active: $active
      name: $name
    ) {
      offset
      limit
      hasMoreData
      totalCount
      results {
        id
        firstName
        lastName
        active
        isReplacementEmployee
        isEmployee
        isAdmin
      }
    }
  }
}
