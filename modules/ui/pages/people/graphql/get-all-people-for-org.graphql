query GetAllPeopleForOrg(
  $orgId: ID!
  $limit: Int
  $offset: Int
  $sortBy: [SortDetailsInput]
  $role: [OrgUserRole]
  $active: Boolean
  $name: String
  $endorsements: [Int]
) {
  orgUser {
    paged(
      orgId: $orgId
      limit: $limit
      offset: $offset
      sortBy: $sortBy
      role: $role
      active: $active
      name: $name
      endorsements: $endorsements
    ) {
      offset
      limit
      hasMoreData
      totalCount
      results {
        id
        rowVersion
        externalId
        firstName
        lastName
        active
        isReplacementEmployee
        isEmployee
        isAdmin
        email
        phoneNumber
        employee {
          id
          primaryPosition {
            id
            name
          }
        }
      }
    }
  }
}
