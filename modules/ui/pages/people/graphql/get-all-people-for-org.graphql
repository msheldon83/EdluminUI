query GetAllPeopleForOrg($orgId: ID) {
  orgUser {
    all(orgId: $orgId) {
      id
      firstName
      lastName
      active
      isReplacementEmployee
      isEmployee
      isAdmin
    }
  }
}
