query GetAllLocationsWithinOrg($orgId: ID, $locationGroups: [Int]) {
  location {
    all(orgId: $orgId, locationGroups: $locationGroups) {
      id
      name
      externalId
      locationGroup {
        id
        name
      }
    }
  }
}
