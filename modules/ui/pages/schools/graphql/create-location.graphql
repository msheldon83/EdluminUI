mutation CreateLocation($location: LocationCreateInput!) {
  location {
    create(location: $location) {
      id
      name
      rowVersion
      externalId
      locationGroupId
      address {
        address1
        city
        state
        postalCode
      }
      phoneNumber
      replacementEndOffsetMinutes
      replacementStartOffsetMinutes
    }
  }
}
