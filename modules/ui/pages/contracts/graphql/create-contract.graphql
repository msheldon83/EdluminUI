mutation CreateContract($contract: ContractCreateInput!) {
  contract {
    create(contract: $contract) {
      id
      name
      rowVersion
      externalId
      numberOfDays
      workDayPatternId
    }
  }
}
