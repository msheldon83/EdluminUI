query GetEmployee($employeeId: ID!) {
  employee {
    byId(id: $employeeId) {
      id
      firstName
      lastName
      primaryPositionId
      primaryPosition {
        id
        name
        needsReplacement
      }
    }
  }
}
