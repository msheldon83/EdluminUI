query GetPositionTypeById($id: ID!) {
  positionType {
    byId(id: $id) {
      name
      externalId
      forPermanentPositions
      forStaffAugmentation
      minAbsenceDurationMinutes
      needsReplacement
      defaultContract {
        id
        name
      }
    }
  }
}
