query GetUserById($id: ID!) {
  user {
    byId(id: $id) {
      id
      allOrgUsers {
        id
        isReplacementEmployee
        organization {
          id
          name
        }
      }
    }
  }
}
