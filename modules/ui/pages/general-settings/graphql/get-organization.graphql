query GetOrganizationById($id: ID!) {
  organization {
    byId(id: $id) {
      id
      rowVersion
      name
      externalId
      timeZoneId
      config {
        absenceSubContact {
          email
          namephone
        }
        absenceEmployeeContact {
          email
          namephone
        }
      }
    }
  }
}
