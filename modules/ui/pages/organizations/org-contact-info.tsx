import * as React from "react";

export const OrganicationContactInfo: React.FC<{}> = props => {
  //Conditional Logic on Org Contact Info

  //Page should show the appropriate contact for the role of the user in each organization

  //If the user is both sub and employee in an organization then show both contacts with labels as shown

  //Unless both contacts are the same, in which case just display the contact once with no label
  //or if only one contact has data, in which case just display the one with no label

  return <></>;
};
