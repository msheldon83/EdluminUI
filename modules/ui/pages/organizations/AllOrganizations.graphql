query AllOrganizations($limit: Int, $offset: Int) {
  organization {
    paged(limit: $limit, offset: $offset) {
      results {
        id
        name
      }
      offset
      limit
      hasMoreData
      totalCount
    }
  }
}
