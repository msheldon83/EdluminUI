query AllOrganizations($limit: Int, $offset: Int) {
  organization {
    paged(limit: $limit, offset: $offset) {
      results {
        id
        name
        config {
          organizationTypeId
        }
      }
      offset
      limit
      hasMoreData
      totalCount
    }
  }
}
