mutation CreateOrganization($organization: OrganizationCreateInput!) {
  organization {
    create(organization: $organization) {
      id
      name
      rowVersion
      externalId
      timeZoneId
    }
  }
}
