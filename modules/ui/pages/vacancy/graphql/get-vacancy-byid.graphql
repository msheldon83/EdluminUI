query GetVacancyById($id: ID!) {
  vacancy {
    byId(id: $id) {
      id
      notesToReplacement
      rowVersion
      details {
        locationId
        workDayScheduleId
        startTimeUtc
        endTimeUtc
        startDate
        endDate
        endTimeLocalTimeSpan
        startTimeLocalTimeSpan
        payCodeId
        vacancyReasonId
        accountingCodeAllocations {
          accountingCodeId
        }
        assignment {
          id
          rowVersion
          employeeId
          employee {
            firstName
            lastName
          }
        }
      }
      startDate
      endDate
      position {
        title
        positionTypeId
        contractId
      }
    }
  }
}
