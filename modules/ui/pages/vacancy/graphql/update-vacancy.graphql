mutation UpdateVacancy($vacancy: VacancyUpdateInput!) {
  vacancy {
    update(vacancy: $vacancy) {
      id
      rowVersion
      details {
        id
        startDate
        assignment {
          id
          rowVersion
          employee {
            id
            firstName
            lastName
            email
          }
        }
      }
    }
  }
}
