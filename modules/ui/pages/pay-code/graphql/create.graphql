mutation CreatePayCode($payCode: PayCodeCreateInput!) {
  orgRef_PayCode {
    create(payCode: $payCode) {
      id
      name
      rowVersion
      externalId
      description
      code
      expired
    }
  }
}
