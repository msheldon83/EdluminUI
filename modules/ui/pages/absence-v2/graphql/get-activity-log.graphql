query GetActivityLogForAbsence($id: ID!) {
  absence {
    activityLog(id: $id) {
      count
      orgId
      activityLogDetails {
        actualUser
        actingUser
        createdUtc
        moreDetail
        title
        subTitle
      }
    }
  }
}
