query GetDeleteAbsenceInfo($absenceId: ID!) {
  absence {
    deletedAbsenceInfo(absenceId: $absenceId) {
      id
      firstName
      lastName
      absenceLocalStartTime
      absenceLocalEndTime
    }
  }
}
