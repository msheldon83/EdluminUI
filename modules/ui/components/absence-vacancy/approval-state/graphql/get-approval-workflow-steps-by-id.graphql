query GetApprovalWorkflowById($id: ID!) {
  approvalWorkflow {
    byId(id: $id) {
      id
      steps {
        stepId
        deleted
        isFirstStep
        isLastStep
        approverGroupHeaderId
        onApproval {
          goto
          args
          criteria
        }
      }
    }
  }
}
