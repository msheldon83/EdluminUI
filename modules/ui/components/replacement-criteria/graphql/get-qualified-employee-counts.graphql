query GetQualifiedEmployeeCountsWithinOrg($orgId: ID!, $positionId: ID!) {
  position {
    qualifiedEmployeeCounts(orgId: $orgId, positionId: $positionId) {
      numFullyQualified
      numMinimallyQualified
    }
  }
}
