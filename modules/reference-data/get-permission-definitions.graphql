query GetPermissionDefinitions($role: OrgUserRole!) {
  permission {
    all(role: $role) {
      id
      displayName
      settings {
        id
        displayName
        levels {
          id
          displayName
        }
      }
    }
  }
}
