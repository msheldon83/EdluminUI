query GetAbsenceReasons($orgId: ID) {
  orgRef_AbsenceReason {
    all(orgId: $orgId) {
      id
      name
      rowVersion
      externalId
      description
      expired
      allowNegativeBalance
      requireNotesToAdmin
      category {
        id
        name
      }
    }
  }
}
