query GetApproverGroups($orgId: ID!) {
  approverGroup {
    all(orgId: $orgId) {
      id
      variesByLocation
      name
      approverGroups {
        id
        name
        location {
          id
          name
        }
      }
    }
  }
}
