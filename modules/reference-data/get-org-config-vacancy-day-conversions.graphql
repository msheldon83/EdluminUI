query GetOrgConfigVacancyDayConversions($orgId: ID!) {
  organization {
    byId(id: $orgId) {
      id
      config {
        vacancyDayConversions {
          name
          maxMinutes
          dayEquivalent
        }
      }
    }
  }
}
